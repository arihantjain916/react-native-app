import {View, Text, ScrollView} from 'react-native';
import React from 'react';
import PasswordCom from './component/PasswordCom';

export default function App() {
  return (
    <ScrollView>
      <PasswordCom />
    </ScrollView>
  );
}
